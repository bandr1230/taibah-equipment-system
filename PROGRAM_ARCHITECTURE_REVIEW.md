# تقرير مراجعة سلامة ومعمارية برنامج إدارة التجهيزات والمخزون

تاريخ المراجعة: 2026-05-31

## الخلاصة التنفيذية

تمت مراجعة مسار تشغيل البرنامج بعد التعديلات الأخيرة، وتم إصلاح كسر ظاهر في صفحة "المستخدمون والصلاحيات" حيث كانت أزرار الإجراء تظهر كنص HTML خام بدل أن تعرض كأزرار فعلية.

سبب الخلل كان في طبقة حماية عرض خلايا الجداول داخل `app.js`: بعد إضافة حماية عامة ضد إدخال HTML غير موثوق، أصبحت الدالة تمنع أي HTML يحتوي على `onclick`، بينما الجداول الحالية في البرنامج تعتمد على أزرار تشغيلية موثوقة تستخدم `onclick` مثل `openModal('user', 1)`.

تم تعديل الحماية بحيث:
- تستمر في تهريب النصوص العادية وغير الموثوقة.
- تمنع `script` و`javascript:` ووسوم HTML الخطرة.
- تسمح فقط بمعالجات `onclick` البسيطة ذات الصيغة الآمنة.
- تسمح فقط بعناصر تشغيلية معروفة داخل الجداول مثل `badge` و`button.btn` و`flex-actions`.

## الملفات التي تم لمسها

- `app.js`: إصلاح منطق HTML الآمن داخل خلايا الجداول.
- `www/app.js`: تم تحديثه عبر `npm run build`.
- `android/app/src/main/assets/public/app.js`: تم تحديثه عبر `npx cap sync android`.
- `PROGRAM_ARCHITECTURE_REVIEW.md`: هذا التقرير.

## نتيجة الاختبارات

- `npm.cmd run build`: ناجح.
- `npm.cmd run test:smoke`: ناجح بعد إعادة التشغيل بالتسلسل.
- `npx.cmd cap sync android`: ناجح.
- تم التأكد أن نسخة `www/app.js` ونسخة Android تحتويان على إصلاح `isTrustedTableHtml`.

ملاحظة: محاولة تشغيل smoke بالتوازي مع build فشلت مؤقتًا لأن build كان يعيد إنشاء مجلد `www` في نفس اللحظة. أعيد الاختبار بالتسلسل ونجح.

## خريطة المعمارية الحالية

### 1. طبقة التشغيل

- `index.html`: نقطة تشغيل الواجهة في المتصفح.
- `www/index.html`: نسخة البناء الثابتة للتوزيع.
- `android/app/src/main/assets/public/index.html`: نسخة Android بعد مزامنة Capacitor.
- `style.css`: الهوية البصرية و RTL والتخطيط العام.
- `manifest.webmanifest` و`service-worker.js`: دعم PWA والتخزين المؤقت.

### 2. طبقة البيانات

- `data.js`: يحتوي نموذج البيانات الافتراضي، الصلاحيات، المستخدمين التجريبيين، والإعدادات الأساسية.
- `db`: كائن التشغيل الأساسي داخل المتصفح.
- `localStorage`: مخزن محلي لحالة البرنامج.
- `supabase-config.js` و`supabase-adapter.js`: طبقة مزامنة اختيارية مع Supabase.
- `supabase_schema.sql`: مرجع مخطط قاعدة البيانات وسياسات Supabase.

### 3. طبقة التطبيق

- `app.js`: الملف المركزي الأكبر، ويحتوي على:
  - التنقل والصفحات.
  - صلاحيات العرض والإجراءات.
  - الحركات والصرف.
  - الدعم بين القطاعات.
  - رفع الاحتياج وشواهد الاحتياج.
  - إدارة التجهيزات.
  - الصيانة.
  - التقارير والتصدير.
  - سجل التدقيق.

يعتمد `app.js` على نمط "طبقات إضافية"؛ توجد دوال أساسية في البداية ثم كتل لاحقة تعيد تغليف أو استبدال بعض الدوال مثل:
- `navItems`
- `getPageTitle`
- `renderPageContent`
- `renderApp`
- بعض دوال الاحتياج والدعم والصيانة.

هذا النمط يعمل، لكنه يحتاج انضباطًا: أي تعديل عام يجب أن يطبق على الدالة المشتركة أو آخر طبقة فعالة.

### 4. محركات المجال

- `need-engine.js`: مرجع حساب وتوليد الاحتياج، ويفصل منطق الكيميائيات والمستهلكات والأجهزة تدريجيًا.
- `ai-analyzer.js`: المحلل الذكي الحالي، وليس المساعد الذكي التوليدي. يملك صفحة المحلل وتحليلاته وسجلات نتائجه.
- `public-asset.js` و`public-asset.html`: عرض عام للأصول عبر الرابط أو QR، ولا يمنح صلاحيات تشغيلية.

### 5. التقارير والمخرجات

- تقارير PDF تستخدم `openPrint` و`reportHtml`.
- تقارير Excel تستخدم `XLSX` من CDN.
- تقرير مسار توليد الاحتياج موجود في `reports/need_generation_workflow_report.pdf`.
- تقارير الاعتماد النهائي للاحتياج أضيفت إلى صفحة إدارة التجهيزات، وتعرض فقط الطلبات المعتمدة نهائيًا.

## الصلاحيات ونطاق البيانات

الصلاحيات الأساسية تمر عبر:
- `hasPermission(permission)`
- `isCentral()`
- فلاتر النطاق مثل `visibleItems`, `visibleTransactions`, `filteredNeeds`, `filteredSupport`, `visibleAuditLogs`.

القاعدة المعمارية الحالية:
- المستخدم العادي يرى نطاق قطاعه وقسمه.
- إدارة التجهيزات أو المدير يرى النطاق المركزي حسب الصلاحية.
- صفحات القائمة تعتمد على `navItems`.
- أزرار الصفحات تعتمد على الصلاحيات داخل HTML.
- دوال التنفيذ الأساسية تتحقق أيضًا من الصلاحية قبل التعديل في أغلب المسارات المهمة.

## المحلل الذكي بعد إزالة المساعد الذكي

تم التأكد أن ملفات المساعد الذكي التوليدي غير محملة في `index.html` و`www/index.html` ونسخة Android.

الحالة الحالية:
- المساعد الذكي التوليدي ملغى من التشغيل.
- صلاحية `use_ai_assistant` تزال من المستخدمين في `data.js`.
- المحلل الذكي الفعال هو `ai-analyzer.js`.
- توجد كتلة قديمة باسم "Analysis Assistant" داخل `app.js` لكنها تعمل كمساعد قراءات تشغيلية داخلي على صفحات تنفيذية، وليست المساعد الذكي التوليدي الملغى.

## تدفق أهم العمليات

### الأصناف والمخزون

1. المستخدم يضيف أو يعدل صنفًا.
2. `saveItem` يتحقق من البيانات ومنع التكرار غير المناسب.
3. يتم تحديث `db.items`.
4. يسجل النظام في `auditLog`.
5. `saveDb` يحفظ محليًا ويستدعي المزامنة إن وجدت.

### الصرف والإدخال

1. `saveTransaction` ينشئ إدخالًا أو طلب صرف.
2. الصرف يتحقق من الرصيد قبل الإنشاء.
3. `approveIssue` يخصم الرصيد عند الاعتماد فقط.
4. الرفض لا يخصم من الرصيد.
5. كل إجراء يسجل في التدقيق.

### الاحتياج

1. المراجع التعليمية وشواهد الاحتياج تغذي حساب الاحتياج.
2. التوليد يبقى في صفحة رفع الاحتياج لتجنب ازدواجية صفحة المراجع.
3. اعتماد القطاع يحيل الطلب إلى إدارة التجهيزات.
4. اعتماد إدارة التجهيزات يحول الطلب إلى حالة معتمدة نهائيًا.
5. صفحة إدارة التجهيزات تعرض تقريرًا خاصًا بالطلبات المعتمدة نهائيًا فقط للتصدير.

### الدعم بين القطاعات

1. القطاع يرفع طلب دعم دون رؤية أرصدة القطاعات الأخرى.
2. إدارة التجهيزات تراجع وتحدد مصدر الدعم حسب الصلاحيات.
3. التنفيذ يخصم من القطاع المانح ويضيف للقطاع الطالب.
4. العرض والتقارير تخضع لتنقيح تفاصيل المصدر والمستفيد حسب الصلاحية.

### الصيانة

1. بلاغات الأعطال والزيارات لها ترقيم مستقل.
2. الزيارة لا تشتق رقمها من البلاغ.
3. مسار الزيارة ينتهي باعتماد الجهة ثم الإغلاق النهائي من إدارة التجهيزات.
4. الإجراءات مربوطة بصلاحيات صيانة دقيقة.

## المخاطر المعمارية المتبقية

1. `app.js` كبير جدًا ويعتمد على إعادة تعريف دوال كثيرة، وهذا يزيد احتمال أن يعدل المطور نسخة غير فعالة.
2. الاعتماد على inline `onclick` واسع؛ تم تأمين عرضه داخل الجداول، لكن الأفضل مستقبلاً الانتقال إلى event delegation.
3. بيانات المستخدمين التجريبية وكلمات المرور ما زالت داخل `data.js` كجزء من نموذج demo.
4. المزامنة مع Supabase مبنية حول حفظ حالة التطبيق، وليست معاملات ذرية لكل عملية؛ هذا قد يسبب last-write-wins عند تعدد المستخدمين.
5. لا توجد اختبارات وحدات كاملة لمسارات الدعم والصيانة والاحتياج، ويوجد فقط smoke check بنيوي.

## التوصية التالية

الأولوية القادمة ليست إضافة ميزات جديدة، بل تقليل مخاطر الصيانة:

1. فصل `app.js` تدريجيًا إلى ملفات حسب المجال: inventory, needs, support, maintenance, reports.
2. تحويل أزرار الجداول من inline `onclick` إلى event delegation.
3. إضافة اختبارات تشغيلية لمسارات:
   - اعتماد الاحتياج.
   - تقرير الطلبات المعتمدة النهائي.
   - طلب الدعم وتنفيذه.
   - بلاغ الصيانة والزيارة والإغلاق.
4. نقل المصادقة وكلمات المرور إلى Supabase Auth أو Backend مستقل قبل الاستخدام الإنتاجي.

## قرار المراجعة

لا يوجد كسر بنيوي ظاهر بعد الإصلاح. البرنامج يبني بنجاح، واجهة الويب متزامنة، ونسخة Android حصلت على أصول التشغيل المحدثة.
