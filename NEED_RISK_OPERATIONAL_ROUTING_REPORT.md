# تقرير تحويل مقياس الاحتياج إلى مقياس جاهزية تشغيلية

## الحالة
تم تحويل صفحة مقياس الاحتياج إلى **مقياس جاهزية المعامل والمقررات** دون تغيير مسار توليد الاحتياج النهائي.

## الفصل بين المسؤوليات
- المراجع التعليمية: توثّق المقرر والتجربة والكميات العلمية.
- مقياس الجاهزية: يراقب قرب الموعد والرصيد الحي ويقترح الإجراء التشغيلي.
- طلبات الاحتياج: هي الصفحة الوحيدة التي تولّد وترفع الاحتياج النهائي.
- الصرف والدعم: ينفذان عبر مساراتهما المعتادة وبحسب الصلاحيات.

## منطق الجاهزية
- يحسب النظام كمية تشغيل الموعد القادم من بيانات المرجع.
- يقرأ أفضل رصيد حي مطابق داخل قطاع المرجع.
- يخصم طلبات الصرف المعلقة من الرصيد المتاح للقرار لتقليل الحجز المزدوج.
- يراعي الاسم والوحدة والمواصفات عند مطابقة سجل المخزون.
- الأجهزة والأدوات القابلة لإعادة الاستخدام تعامل كطاقة تشغيلية، ولا ينشأ لها صرف دائم إذا كانت متاحة.
- الاستخدامات المتزامنة للأجهزة والأدوات القابلة لإعادة الاستخدام تجمع داخل نفس الفترة.

## مسارات المعالجة
- الرصيد المحلي كاف للمادة المستهلكة: طلب صرف.
- الرصيد المحلي يغطي جزءًا فقط: طلب صرف للمتاح وطلب دعم للعجز.
- الرصيد المحلي غير كاف: طلب دعم إلى إدارة التجهيزات.
- الجهاز أو الأداة القابلة لإعادة الاستخدام متاحة: جاهز للتشغيل.
- طاقة الجهاز أو الأداة غير كافية: طلب دعم أو إعارة.
- وجود طلب مرتبط: يظهر كطلب قيد المعالجة ولا يسمح بتكرار نفس نوع الطلب.

## الربط ومنع التكرار
طلبات الصرف والدعم المنشأة من الصفحة تحفظ الحقول:
- relatedReferenceId
- relatedReferenceNo
- relatedOccurrenceKey
- relatedCourseName
- relatedExperimentName
- relatedExperimentDate
- sourceContext = need_risk_readiness

ويستخدم `relatedOccurrenceKey` لمنع تكرار طلب صرف أو دعم لنفس المرجع والموعد التشغيلي.

## حماية خصوصية الدعم
- لا تفحص الصفحة أرصدة القطاعات الأخرى للمستخدم العادي.
- لا تعرض القطاع المانح أو مصدر الدعم.
- طلب الدعم يرسل إلى إدارة التجهيزات بحالة قيد المراجعة.
- اختيار المصدر وتنفيذ التحويل يبقيان داخل مسار الدعم المعتمد.

## تغييرات الواجهة
- تغيير اسم القائمة إلى: جاهزية المعامل.
- تغيير عنوان الصفحة إلى: مقياس جاهزية المعامل والمقررات.
- إزالة إضافة المرجع وتوليد الاحتياج من الصفحة.
- إضافة مؤشرات: جاهزة، تحتاج صرف، تحتاج دعم، معالجة مشتركة، قيد المعالجة، حرجة أو متأخرة.
- إضافة إجراءات طلب صرف وطلب دعم داخل الصف بحسب الحالة والصلاحية.

## قواعد الحماية
- لا يتم الصرف أو الدعم تلقائيًا.
- لا يتم خصم الرصيد عند إنشاء طلب الصرف؛ الخصم بعد الاعتماد المعتاد.
- لا يتم تحديد مصدر الدعم عند إنشاء الطلب من القطاع.
- لا يمكن إنشاء إجراء من حساب لا يتبع قطاع المرجع.
- لا يتم إنشاء احتياج نهائي من صفحة الجاهزية.

## الملفات المعدلة
- app.js
- style.css
- index.html لتحديث رقم نسخة ملفات التشغيل
- service-worker.js لتحديث مفتاح الكاش
- www عبر عملية البناء
- أصول Android عبر المزامنة

## الاختبارات
- `node --check app.js`
- `node --check data.js`
- `node --check need-engine.js`
- `npm.cmd run build`
- `npm.cmd run test:smoke`
- `npx.cmd cap sync android`
- بناء وفحص APK داخل `taibah-apk`

## منع تضارب الأصناف والمواصفات
- لا تعتبر المطابقة الجزئية للاسم وحدها كافية لقراءة الرصيد أو اختيار مصدر الدعم.
- يستخرج النظام المقاس والسعة والتركيز والأرقام والوحدات والمواد المميزة من اسم الصنف وحقول المواصفات.
- لا يطابق الصنف العام مثل `سحاحة` الصنف المحدد مثل `سحاحة 50 مل` في أي اتجاه.
- لا يطابق مقاسًا مختلفًا مثل `سحاحة 25 مل` مع `سحاحة 50 مل`.
- يعرض جدول الجاهزية اسم سجل المخزون المطابق أسفل الرصيد الحي.
- يتحقق مسار تحديد مصدر الدعم والتنفيذ من هوية الصنف والمقاس والمواصفات داخل الدالة نفسها، وليس من قائمة الاختيار فقط.

## القرار
الصفحة أصبحت مؤشر جاهزية تشغيلي مرتبطًا بالمقررات والمواعيد، بينما بقي توليد الاحتياج النهائي في صفحة طلبات الاحتياج فقط.
